# === 核心依赖 ===
# Python 3.11/3.12（不支持 3.13，torch 尚未支持）
pytest>=7.0,<9.0
pytest-asyncio>=0.23,<1.0

# === AI 模型 ===
anthropic>=0.40.0
openai>=1.12.0,<2.0.0
volcengine-python-sdk[ark]>=1.0.0

# === 嵌入模型（本地向量化）===
# sentence-transformers 3.x 与 torch 2.x 兼容，5.x 太新可能有兼容性问题
sentence-transformers>=3.0.0,<4.0.0
torch>=2.2.0,<3.0.0
numpy>=1.21.0,<2.0.0
faiss-cpu>=1.7.4

# === 向量数据库 ===
# 本地模式（推荐）：QDRANT_MODE=local + QDRANT_STORAGE_PATH
qdrant-client>=1.7.0,<2.0.0

# === Web 框架 ===
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# === 工具库 ===
pydantic>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.25.0
json-repair>=0.30.0
